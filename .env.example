# Example environment variables for local development
# Copy this file to .env and fill in the values

# --- Django Core ---
# IMPORTANT: Generate a new, strong secret key for production
DJANGO_SECRET_KEY=your_secret_key
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# --- Frontend ---
# The URL of your React frontend (for CORS and password reset links)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Database ---
# (Optional) Uncomment to use PostgreSQL instead of sqlite
# DATABASE_URL=postgres://user:password@localhost:5432/noor_db

# --- Email ---
# (Optional) For development, Django uses the console backend by default
# For production, you would set SMTP variables here.
DEFAULT_FROM_EMAIL=noreply@noorinstitute.com

# --- Sentry ---
# (Optional) For error tracking in production
SENTRY_DSN=